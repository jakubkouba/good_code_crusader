<%= render 'refinery/admin/search_header', :url => refinery.resumes_admin_employments_path %>
<div class='pagination_container'>
  <% if @employments.any? %>
    <%= render 'employments' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
