<% content_for :body do %>
  <ul id="resumes">
    <% @resumes.each do |resume| %>
      <li>
        <%= link_to resume.title, refinery.resumes_resume_path(resume) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
